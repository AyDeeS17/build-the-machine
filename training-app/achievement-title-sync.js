(()=>{
'use strict';
const CLAIM_KEY='btm_achievement_claims_v1',TITLE_KEY='btm_equipped_title_v1';
const read=(k,d)=>{try{return JSON.parse(localStorage.getItem(k)||'null')??d}catch{return d}};
const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const iconFor=id=>({first_rep:'✦',no_excuses:'◈',clean_machine:'✧',iron_week:'▣',halfway:'◇',perfect_week:'★',consistent:'▥',legend:'♛'}[id]||'◆');
function models(){return window.BTM_ACHIEVEMENTS?.models?.()||[]}
function claims(){const c=read(CLAIM_KEY,{});return c&&typeof c==='object'?c:{}}
function renderTitleSection(){const header=document.querySelector('.app-header');if(!header)return;let el=document.getElementById('titleSection');if(!el){el=document.createElement('section');el.id='titleSection';el.className='title-section';header.insertBefore(el,header.querySelector('.main-nav')||null)}const ms=models(),claimed=claims(),available=ms.filter(a=>claimed[a.id]===true),equipped=localStorage.getItem(TITLE_KEY)||'';const sig=JSON.stringify({equipped,available:available.map(a=>[a.id,a.titleReward])});if(el.dataset.sig===sig)return;el.dataset.sig=sig;el.innerHTML=`<div class="achievement-title-kicker">TITLE</div><select class="achievement-title-select" aria-label="Choose equipped title"><option value="">NO TITLE EQUIPPED</option>${available.map(a=>`<option value="${esc(a.titleReward)}" ${equipped===a.titleReward?'selected':''}>${esc(iconFor(a.id)+'  '+a.titleReward)}</option>`).join('')}</select>`;const select=el.querySelector('.achievement-title-select');select.onchange=()=>{if(select.value)localStorage.setItem(TITLE_KEY,select.value);else localStorage.removeItem(TITLE_KEY);renderTitleSection()}}
function decorateCards(){const root=document.getElementById('achievementList');if(!root)return;const ms=models(),byId=new Map(ms.map(a=>[a.id,a]));root.querySelectorAll('.achievement-card').forEach(card=>{const a=byId.get(card.dataset.achievementId);if(!a)return;let reward=card.querySelector('.achievement-title-reward');if(!reward){reward=document.createElement('div');reward.className='achievement-title-reward';const progress=card.querySelector('.achievement-progress');(progress||card.querySelector('.achievement-note'))?.insertAdjacentElement('beforebegin',reward)}reward.innerHTML=`TITLE REWARD <strong>${esc(a.titleReward)}</strong>`})}
function inject(){if(document.getElementById('achievementTitleSyncCSS'))return;const s=document.createElement('style');s.id='achievementTitleSyncCSS';s.textContent=`.achievement-title-kicker{font:800 8px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.16em;color:var(--muted);margin-bottom:6px}.achievement-title-select{width:100%;min-height:42px;padding:9px 34px 9px 12px;border:1px solid var(--line);border-radius:11px;background:#0b171f;color:#fff;font:900 11px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.08em;outline:none;cursor:pointer}.achievement-title-select:focus{border-color:#66b9df;box-shadow:0 0 0 2px rgba(102,185,223,.12)}.achievement-title-select option{background:#0b171f;color:#fff;font:900 11px ui-monospace,SFMono-Regular,Menlo,monospace}.achievement-title-reward{grid-column:2;margin-top:6px;color:var(--muted);font:800 7px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.09em}.achievement-title-reward strong{color:#9fdcf5;font-weight:900}.achievement-card.claimed .achievement-title-reward strong{color:#91e19d}`;document.head.appendChild(s)}
function boot(){inject();renderTitleSection();decorateCards();setInterval(()=>{renderTitleSection();decorateCards()},700)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
