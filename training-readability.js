/* Training readability layer. Presentation only, does not alter progression data. */
(()=>{
'use strict';
if(window.__BTM_TRAINING_READABILITY__)return;
window.__BTM_TRAINING_READABILITY__=1;
const css=`
.training-prescription{display:flex;align-items:stretch;gap:10px;margin-top:10px;flex-wrap:wrap}.training-volume,.training-rir{display:flex;flex-direction:column;justify-content:center;min-width:110px;padding:8px 12px;border:1px solid #294656;border-radius:6px;background:linear-gradient(145deg,#12232d,#09141b);box-shadow:inset 0 1px 0 rgba(255,255,255,.025)}.training-volume-label,.training-rir-label{font:8px 'JetBrains Mono';letter-spacing:.14em;color:#8195a3;text-transform:uppercase}.training-volume-value{margin-top:2px;color:#eaf5f9;font:700 clamp(20px,2.3vw,27px) 'JetBrains Mono';line-height:1}.training-rir-value{margin-top:2px;color:#66b9df;font:700 clamp(18px,2.1vw,24px) 'JetBrains Mono';line-height:1}.exercise .target{font-size:inherit!important;margin-top:8px!important;color:#8195a3!important;text-transform:none!important}.exercise .target .training-prescription{margin-top:0}.exercise .target .training-volume{border-color:rgba(102,185,223,.28)}.exercise .target .training-rir{border-color:rgba(79,174,159,.25)}.exercise.done .target .training-volume{border-color:rgba(79,174,159,.42)}
@media(max-width:600px){.training-prescription{gap:7px}.training-volume,.training-rir{min-width:92px;padding:7px 9px}.training-volume-value{font-size:19px}.training-rir-value{font-size:18px}}
@media(prefers-reduced-motion:reduce){.training-volume,.training-rir{transition:none!important}}
`;
function install(){if(document.getElementById('btm-training-readability-css'))return;const s=document.createElement('style');s.id='btm-training-readability-css';s.textContent=css;document.head.appendChild(s)}
function enhance(){const root=document.getElementById('workouts');if(!root)return;root.querySelectorAll('.exercise .target').forEach(target=>{if(target.dataset.readable==='1')return;const text=(target.textContent||'').trim();const parts=text.split(/\s*·\s*/);if(parts.length<2)return;const volume=parts[0].trim(),rir=parts.slice(1).join(' · ').replace(/\bRIR\b/i,'').trim();const m=volume.match(/^([^×x]+)[×x](.+)$/);const sets=m?m[1].trim():'';const reps=m?m[2].trim():volume;target.textContent='';const wrap=document.createElement('span');wrap.className='training-prescription';const volumeBox=document.createElement('span');volumeBox.className='training-volume';volumeBox.innerHTML=`<span class="training-volume-label">SETS · REPS</span><span class="training-volume-value">${sets?sets+' × ':''}${reps}</span>`;const rirBox=document.createElement('span');rirBox.className='training-rir';rirBox.innerHTML=`<span class="training-rir-label">RIR</span><span class="training-rir-value">${rir||'1–2'}</span>`;wrap.append(volumeBox,rirBox);target.appendChild(wrap);target.dataset.readable='1'})}
function boot(){install();enhance();const root=document.getElementById('workouts');if(!root||root.__btmReadabilityObserver)return;const observer=new MutationObserver(()=>{observer.disconnect();enhance();observer.observe(root,{childList:true,subtree:true})});observer.observe(root,{childList:true,subtree:true});root.__btmReadabilityObserver=observer}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
